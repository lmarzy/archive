/* start-namespace */
var project = project || {};
project._module = project._module || {};
project._service = project._service || {};
/* end-namespace */


(function () {
  'use strict';

	project._module.module.init();

	// if ( $('form').length !== 0 ) {
	// 	ppr._module.forms.init( $('form') );
	// }


})();
